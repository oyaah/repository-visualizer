# Flask Dogfood Run - 2026-06-30

Repository: `pallets/flask`

Source checkout: `/Users/yashbansal/Documents/Codex/2026-06-28/ok/work/repos/flask`

## Analyzer Output

- Files analyzed: 83
- Local edges: 176
- Entry points: 8
- Start-here findings: 5
- Truncated: no

## Useful Findings

- `src/flask/app.py` is both a dependency-cycle participant and the highest-complexity file.
- `src/flask/__init__.py` is a high-impact hub imported by 44 files.
- `src/flask/cli.py` is detected both from its Python main guard and from `pyproject.toml` script metadata.
- Tests dominate file size: `tests/test_basic.py` is 1483 LoC.

## Fixes Found By Dogfooding

- `from . import Flask` and similar package-symbol imports now resolve to package `__init__.py` when no sibling module exists.
- Generic route decorators such as `@bp.get(...)` are labeled as Python web routes instead of being falsely labeled FastAPI.

## Product Read

The tool is useful on Flask because it gives a starting path instead of a wall of files:

1. Start with `src/flask/cli.py` and `src/flask/app.py`.
2. Use hub/issue presets to reduce the graph.
3. Use blast radius on `src/flask/__init__.py` before changing public exports.
